{% extends "layout/base.php.twig" %}
{% block content %}
    <h1>{{post.title}}</h1>
    {{ function('do_shortcode', '[hr]') }}
    <img src="{{post.thumbnail.src}}" />
    <div class="body">
        {{post.content}}
    </div>
{% endblock %}